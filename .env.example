PORT=3000
JWT_SECRET=your_jwt_secret_here


# Database (MySQL)

DB_HOST=localhost
DB_PORT=3306
DB_NAME=alumni_platform
DB_USER=root
DB_PASSWORD=1234

# Optional (if using Sequelize / TypeORM / etc.)

DB_DIALECT=mysql

# JWT (if your project uses auth)

JWT_SECRET=your_secret_key

# Environment

NODE_ENV=development
