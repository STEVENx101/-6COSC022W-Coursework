# Alumni Influencer API (Day 1)

## Setup

1. Install dependencies
   npm install

2. Run server
   npm run dev

## Endpoints

POST /api/auth/register
POST /api/auth/login

## Notes
- Uses in-memory DB (temporary)
- Will be replaced with MySQL in later days